import { Injectable } from "@nestjs/common";

// TODO: its a temporary service, after the implement a first auth provider (credential) if i need this service for creating user, i fill this for using
@Injectable()
export class AuthAccountService {
    constructor(

    ) { }

    // TODO: create : with custom fields, phone,email,username
    // TODO: exist :
}